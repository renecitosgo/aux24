/*import Formulario2 from "./Formulario2"

const Carroborrar = ()=>{

    const carreto = ["1", "2", "3"]
    console.log(carreto.length)

    return(<div>
        {carreto.length > 0 ? <Formulario2/> : <h3>"No tenés productos en tu carrito"</h3> }
        {carreto.map((producto, indice)=>{
            return <p key={indice}>{producto}</p>
        })}
        

        </div>
    )
}

export default Carroborrar*/
